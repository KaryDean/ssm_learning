<?xml version="1.0" encoding="UTF-8"?><!DOCTYPE configuration PUBLIC "-//mybatis.org//DTD Config 3.0//EN"        "http://mybatis.org/dtd/mybatis-3-config.dtd"><configuration>    <properties resource="jdbcConfig.properties"/>        <settings>        <setting name="cacheEnabled" value="true"/>    </settings>        <typeAliases>        <!--前面我们提到，在使用Integer作为返回值时，可以使用int或者Int都是可以的，是因为使用了取别名的方法-->        <!--使用typeAlias取别名的时候，不区分大小写，所有我们在使用user作为resultType的时候，可以写成uSer都是可以的-->        <!--<typeAlias type="com.kearmy.domain.User" alias="user"/>-->        <!--有时候为了方便，我们对改包下的所有类取别名-->        <package name="com.kearmy.domain"/>    </typeAliases>    <environments default="mysql">        <environment id="mysql">            <transactionManager type="JDBC"></transactionManager>            <dataSource type="POOLED">                <property name="driver" value="${driver}"/>                <property name="url" value="${url}"/>                <property name="username" value="${username}"/>                <property name="password" value="${password}"/>            </dataSource>        </environment>    </environments>    <mappers>        <!--<mapper resource="com/kearmy/dao/IUserDao.xml"/>-->        <!--用于指定存放dao接口所在的包，使用了之后，不需要在使用mapper以及resource或者class了-->        <package name="com.kearmy.dao"/>    </mappers></configuration>