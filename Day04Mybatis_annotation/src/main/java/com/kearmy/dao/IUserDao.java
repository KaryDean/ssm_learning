package com.kearmy.dao;import com.kearmy.domain.User;import org.apache.ibatis.annotations.*;import org.apache.ibatis.mapping.FetchType;import java.util.List;/** * 注解开发 * 支持@Select @Insert @Update @Delete */public interface IUserDao {    /**     * 查询所有用户信息     * @return     */    @Select("select * from user")    @Results(id = "userMap", value = {            @Result(id = true, column = "id", property = "userId"),            @Result(column = "username", property = "userName"),            @Result(column = "address", property = "userAddress"),            @Result(column = "sex", property = "userSex"),            @Result(column = "birthday", property = "userBirthday"),            @Result(column = "id", property = "accounts",                    many = @Many(select = "com.kearmy.dao.IAccountDao.findAccountByUid",                            fetchType = FetchType.LAZY))    })    List<User> findAll();    /**     * 通过id查询用户     * @param uid     * @return     */    @Select("select * from user where id=#{uid}")    @ResultMap(value = {"userMap"}) // 可以省略至    @ResultMap("userMap")    User findOne(Integer uid);}